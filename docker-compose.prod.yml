version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: aods-postgres-prod
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-aods_dev_v3}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "8004:5432"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
      - ./aods_dev_v3.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - aods-network-prod

  # PgAdmin
  pgadmin:
    image: dpage/pgadmin4
    container_name: aods-pgadmin-prod
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@admin.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
    ports:
      - "8005:80"
    volumes:
      - pgadmin_data_prod:/var/lib/pgadmin
    restart: always
    networks:
      - aods-network-prod

  # Backend Server
  server:
    build:
      context: ./server
      dockerfile: Dockerfile.prod
    container_name: aods-server-prod
    ports:
      - "8066:8066"
    env_file:
      - ./.env.local.docker
    environment:
      - NODE_ENV=production
      - DATABASE_HOST=postgres
      - POSTGRES_PORT=5432
      - PORT=8066
      - FRONTEND_URL=${FRONTEND_URL:-http://203.154.2.149:8060}
    working_dir: /app
    command: npm run start
    depends_on:
      postgres:
        condition: service_healthy
    restart: always
    networks:
      - aods-network-prod

  # Frontend Client
  next-app:
    build:
      context: ./client/my-app
      dockerfile: Dockerfile.prod
      args:
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://203.154.2.149:8066}
    container_name: aods-client-prod
    ports:
      - "8060:8060"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL:-http://203.154.2.149:8066}
      - SERVER_API_URL=http://server:8066
    working_dir: /app
    command: npm run start -- -p 8060
    depends_on:
      - server
    restart: always
    networks:
      - aods-network-prod

volumes:
  postgres_data_prod:
  pgadmin_data_prod:

networks:
  aods-network-prod:
    driver: bridge

